start
  float a; 
  int b 
  float a,x,total; 
  a = 2.0;
  x = 1;
  scan (b);
  scan (y)
  result = (a*b ++ 1) % a;
  else;
  print {Resultado: };
  print (result);
  print ({Total: });
  total = y / x;
  print ({Total: };
  print (total);
exit